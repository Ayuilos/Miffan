# Upstream synchronization — 2026-08-28

## Scope

- Fork base: `e5e2c837653f9d8fad8b37396197ab1ff29d4993` (local `master`).
- Previous upstream point: `170a612e9ee8e11fb181b3059f9208f13b2c334a`.
- Reviewed upstream end: `7aa909b8997947bd2ac02ca5d661746101271333`.
- Last published Miffan release: `3.0.4` (`fc99544a1aa0b335c30b60595212e875295afcba`).
- Preserve merge ancestry for all 12 upstream commits; include only the reviewed effective changes below.
- Preserve the three existing local commits for conversation artifact directories, mascot motion, and queued messages/floating chat controls.

## Included upstream changes

| Commit | Effective change |
| --- | --- |
| `53dc38b8` | Remove unused, fully commented-out MCP transport implementations; active SDK transports are unchanged. |
| `8f4f1286` | Extract translation orchestration into `TranslationHandler`; adapt dependency injection and keep Miffan's selected-text/model-switch behavior. |
| `7714f2bc` | Import Chatbox Backup v2 ZIP archives, provider/model settings, images, system prompts, and supported message alternatives; skip already imported conversations before saving images. |
| `0533acde` | Stop adding the RikkaHub provider to new/default provider lists. Existing saved providers are not removed by the settings loader. |
| `979cd169` | Update the Kimi provider icon. |
| `6e26affe` | Add preset MiMo TTS voices while retaining custom voice input. |

## Excluded or deferred changes

| Commit | Reason |
| --- | --- |
| `ca31612d`, `7aa909b8` | Keep Miffan's input layout, attachment picker, message queue, and floating chat controls. Do not add the unused upstream attachment-picker component. |
| `da5fd77b` | Upstream version `2.4.14` / code `181` is not part of Miffan's independent version line. |
| `8a9f4f0f` | Keep Miffan's own settings/community links and branding. |
| `9687f97a` | Keep Miffan's isolated nightly workflow, schedule, and signer checks. |
| `eba2e96c` | Defer the dependency bundle, including Material 3 alpha, baseline-profile RC, and SQLite vector 1.0.0, pending dedicated compatibility validation. |

These commits become ancestors of the sync merge even when their content is excluded. Future syncs must explicitly revisit deferred changes; ancestry alone must not be interpreted as feature adoption.

## Miffan-specific conflict resolutions

- Keep namespace `me.ayuilos.miffan`, application ID `me.ayuilos.miffan.app`, signing, deep links, update source, database schema, and release workflows unchanged.
- Translate new Kotlin sources and tests into the Miffan namespace; keep shared SDK namespaces unchanged.
- Preserve `ChatService` queue ownership, workspace tools/scopes, reply-completion events, and all mascot behavior.
- Preserve the old Chatbox JSON parser as `LegacyChatboxImporter`. Detect ZIP by content, not the temporary filename, and accept both JSON and ZIP in the picker. Legacy conversation/message UUIDs and duplicate skipping remain compatible.
- Keep the upstream v2 importer limits and archive-path validation; test duplicate-image avoidance, unsafe paths, and unsupported format versions.
- Keep the existing Miffan version during this sync. Select the next version separately in release preparation.

## Verification

- The online `./gradlew test lint assembleDebug :app:assembleDebugAndroidTest --stacktrace` completed 300 app JVM tests and built the Debug APK/test APK. Lint then waited in a Maven-metadata TLS handshake, confirmed by a Gradle thread dump; that invocation was interrupted.
- Passed `./gradlew --offline test lint assembleDebug :app:assembleDebugAndroidTest --stacktrace` in 2m 53s using the existing dependency cache, without disabling Lint checks or changing source/dependencies. Remote pull-request CI must still run the normal online command.
- Passed all 12 focused emulator tests: legacy Chatbox import, floating chat controls, and Miffan motion on API 35 with 16 KB memory pages. Reviewed the generated theme/mascot screenshots; the temporary emulator was closed without saving changes.
- Confirm no effective diff in app identity/version/signing, database migrations, dependency versions, release/nightly workflows, or the preserved chat/settings UI files.
- Pull-request CI and Miffan review are still required before merging into remote `master`.
- Production signing, provenance verification, and in-place upgrade acceptance remain separate release gates.

## Tag safety

The first `origin --tags` fetch reported a pre-existing local/remote `nightly` tag mismatch. Neither tag was overwritten. Origin branches were refreshed with `--no-tags`; GitHub Releases and the remote `3.0.4` tag were checked directly. Upstream was fetched with `--no-tags`, and no upstream tags will be propagated to origin.
