# Selective upstream sync — 2026-09-17

Miffan base: 4f69d5d2 (workspace introduction and documentation included).
Upstream common base: 08c2648b5a58c79e5eec9d39af1ffc85eb07e939.
Reviewed upstream end: a7850967f455389da386ac4c0d8086ff2ceb4394.
Previous adaptation records: 2026-09-08.md and 2026-09-14.md.

Selected patches, manually adapted to the Miffan namespace:

- 5902feab7e123b89c72733cb7f9d47a81b7a4f4a: omit blank text parts from attachment-only messages; preserve text added while editing an image-only message. Keep Miffan's `isEmptyInputMessage()` predicate, which also protects queued drafts. Add regression tests for sending images and adding/removing captions.
- a7850967f455389da386ac4c0d8086ff2ceb4394: disable inline-code ligatures in both Markdown renderers and simple HTML rendering, complementing the previously integrated code-block fix.

Excluded/deferred areas:

- Previously adapted picker, export, keyboard, statistics, provider-name, code-block and web/tool-detail fixes are not reapplied.
- d47d13a6 says regeneration deletes all later messages. Miffan preserves conversation branches, so that wording would be misleading.
- c8853531 replaces the JavaScript runtime and native dependency across app/search; defer to a separate runtime compatibility cycle.
- 5078ce19 changes MiniMax's default from a turbo model to an HD model, affecting service selection and cost; defer to a provider-focused update.
- Blur/glass composer effects, Haze and broad dependency changes, Volcengine TTS, voice mode, backup replacement, provider/session requests, model registry changes and navigation changes remain separate feature/compatibility work.
- Upstream workspace paths, AGENTS.md access and terminal changes require Miffan scope/permission adaptation and must not overwrite the tested local/SSH implementation.
- Upstream release numbers, branding and sponsor changes are excluded.

No package identity, signing, database schema, migrations or download endpoints change in this sync.
Provenance: both patches belong in **Synced from RikkaHub**. Topology alone cannot classify these namespace adaptations; the exact source SHAs above are the audit evidence.
