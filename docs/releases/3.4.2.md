更新内容：

### Miffan 自有改动

- 远程工作空间复用 SSH 连接，打开时预连接，并在空闲后自动释放。
- 离开远程终端页面后保留会话和滚动记录，返回时可继续使用；连接状态与手动断开操作更清晰。
- 取消或超时只中断对应的远程操作，改善断线和页面切换时的稳定性。
- AI 工作空间工具在生成时绑定执行目标，明确区分准备中与历史调用，降低切换工作空间后误操作的风险。

### 同步自 RikkaHub

- 修复部分模型接口不接受工具结果中多余字段的问题。
- 加快大图片裁剪后的确认速度。
- 修复删除收藏后点击“撤销”未正确恢复的问题。
- 修复半透明思考气泡中步骤节点出现背景方块的问题。

Updates:

### Miffan changes

- Reuse SSH connections across remote workspace operations, connect when opening a workspace, and release idle connections automatically.
- Keep remote terminal sessions and scrollback when leaving the page, with clearer connection status and manual disconnect controls.
- Limit cancellation and timeouts to the affected remote operation, improving reliability across disconnects and navigation.
- Bind AI workspace tools to their execution target during generation and distinguish preparing calls from historical ones.

### Synced from RikkaHub

- Fix compatibility with model endpoints that reject an extra field in tool results.
- Speed up confirmation after cropping large images.
- Fix Undo not restoring a deleted favorite correctly.
- Remove background squares from step nodes in translucent reasoning bubbles.
