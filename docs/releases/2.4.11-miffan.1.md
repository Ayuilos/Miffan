更新内容：

### Miffan 自有改动

- 升级 Miffan 助手形象系统：默认使用动态米饭头像，并支持外观预设、角色类型与动作风格配置。
- 新增面向不同角色和场景的个性动作，优化饺子角色的标志性动作表现，并让角色配色跟随应用主题。
- 更新检查切换到 Miffan 自有 GitHub Releases，可识别并选择对应的 arm64 安装包。
- 新增 OpenRouter 文本转语音服务。
- 改进模型能力识别，保留动态发现的多模态能力，并兼容强制推理模型的请求要求。

### 同步自 RikkaHub

- 新增网络设置页，可自定义 User-Agent，并配置带认证的 HTTP/SOCKS5 代理和连接测试。

Updates:

### Miffan changes

- Upgraded the Miffan assistant character system with animated avatars by default, plus appearance presets, character types, and motion styles.
- Added character- and context-specific behaviors, refined the dumpling character’s signature motion, and synchronized character colors with the app theme.
- Switched update checks to Miffan’s own GitHub Releases, including selection of the matching arm64 package.
- Added OpenRouter text-to-speech support.
- Improved model capability detection to preserve dynamically discovered multimodal support and handle models that require reasoning.

### Synced from RikkaHub

- Added network settings for a custom User-Agent, authenticated HTTP/SOCKS5 proxies, and connection testing.
