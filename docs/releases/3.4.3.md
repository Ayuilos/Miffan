更新内容：

### Miffan 自有改动

- 远程工作区中新建的对话成果默认整理到 `.miffan/conversations`，旧文件仍可在原路径访问。
- 调整夜间模式下蓝色大肥鱼的脸部颜色，与官网保持一致。

### 同步自 RikkaHub

- 改善 Google 与部分 OpenAI 兼容接口的工具调用，减少参数格式导致的请求或回复失败。
- 忽略未填写名称的 MCP 请求头，避免连接报错。
- 修复编辑工作区大文件后切到后台时可能崩溃的问题。
- 复制带行号的代码块时不再混入行号。

Updates:

### Miffan changes

- Organize new remote workspace conversation files under `.miffan/conversations`; existing files remain accessible at their original paths.
- Match Whale Girl's face color in dark mode to the website.

### Synced from RikkaHub

- Improve tool calls with Google and some OpenAI-compatible endpoints, reducing failures caused by tool argument formats and response events.
- Ignore MCP request headers with empty names to prevent connection errors.
- Fix a possible crash when a large workspace file is being edited and the app moves to the background.
- Exclude line numbers when copying a code block that shows them.
