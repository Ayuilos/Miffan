更新内容：

### Miffan 自有改动

- 新增远程工作空间，通过 SSH 连接服务器或已接入的 Tailscale 私网主机，管理多台主机和多个项目目录。
- 支持远程文件浏览、编辑、导入导出及交互式终端，也可让 AI 在绑定的工作空间读写文件、执行命令。
- 新增 SSH 密钥管理，支持生成、导入、复制公钥、查看私钥以及加密或不加密导出。
- 简化工作空间管理界面，通过图标和连接状态区分本地与远程，并按执行目标管理 AI Shell 权限与确认。
- 修复首次打开远程文件页的连接报错、正式包导入 SSH 私钥失败，以及文件预览返回后跳到根目录的问题；增加远程文件操作超时处理。
- 新增远程工作空间功能介绍弹窗，并更新 README 截图与内置帮助文档。

### 同步自 RikkaHub

- 修复纯图片消息携带空文本的问题，并保留编辑纯图片消息时新增的文字。
- 关闭内联代码连字，让代码符号保持原样显示。

Updates:

### Miffan changes

- Add remote workspaces over SSH for servers and hosts on an already connected Tailscale network, with multiple hosts and project directories.
- Browse, edit, import and export remote files, use interactive terminals, and let AI read files and run commands in its assigned workspace.
- Add SSH key management with generation, import, public-key copying, private-key viewing, and encrypted or unencrypted export.
- Simplify workspace management, distinguish local and remote workspaces with icons and connection status, and scope AI Shell permissions and confirmations to the execution target.
- Fix initial remote-file connection errors, SSH private-key import failures in release builds, and file previews returning to the workspace root; add timeouts for stalled remote file operations.
- Add a remote-workspace introduction dialog and update README screenshots and built-in help documentation.

### Synced from RikkaHub

- Fix empty text parts in image-only messages and preserve captions added while editing them.
- Disable inline-code ligatures so code symbols retain their original appearance.
