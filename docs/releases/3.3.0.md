更新内容：

### Miffan 自有改动

- 本次无新增面向用户的自有改动，3.3.0 预览系列现进入正式发布。

### 同步自 RikkaHub

- 修复模型和提供商名称输入空格时被立即删除的问题，改为保存时清理首尾空格。
- 修复统计页面遇到损坏消息数据时崩溃的问题，保留原始数据并跳过无法解析的记录。
- 网页端问答支持多选，并可附加自定义文字回复。
- 代码块关闭字体连字，让代码符号保持清晰可辨。
- 工具仍在执行时即可打开调用详情。

Updates:

### Miffan changes

- No new fork-owned user-facing changes since the latest preview; the 3.3.0 preview series is now a stable release.

### Synced from RikkaHub

- Preserve spaces while editing model and provider names, trimming leading and trailing spaces only when saving.
- Prevent statistics from crashing on malformed message data, preserving the original records and skipping entries that cannot be parsed.
- Support multiple selections and additional custom text in web questions.
- Disable font ligatures in code blocks to keep code symbols distinct.
- Allow opening tool-call details while a tool is still running.
